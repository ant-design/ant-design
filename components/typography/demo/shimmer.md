## zh-CN

文字闪烁效果，常用于展示"正在思考/加载"状态。可以通过 `duration` 控制动画速度，通过 `disabled` 禁用动画。

## en-US

Shimmering text effect, commonly used to display "thinking/loading" status. Use `duration` to control animation speed, and `disabled` to disable animation.
