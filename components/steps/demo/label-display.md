## zh-CN

使用 `labelDisplay` 控制标签展示方式。`wrap` 允许标签换行，`scroll` 允许横向滚动。

## en-US

Use `labelDisplay` to control label rendering. `wrap` allows multi-line labels, and `scroll` enables horizontal scrolling.
