import type { TimePickerLocale } from '../index';

const locale: TimePickerLocale = {
  placeholder: 'वेळ निवडा',
  rangePlaceholder: ['प्सुरुवातीचा वेळ', 'समाप्तीचा वेळ'],
};

export default locale;
