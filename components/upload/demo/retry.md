## zh-CN

上传失败后点击重试图标可重新上传。通过 `showUploadList.showRetryIcon` 显示重试图标，点击后触发 `onRetry` 回调并重新发起上传请求。

## en-US

Retry a failed upload by clicking the retry icon. Enable it with `showUploadList.showRetryIcon`; clicking the icon fires the `onRetry` callback and re-sends the upload request.
