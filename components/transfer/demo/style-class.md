## zh-CN

通过 `classNames` 和 `styles` 传入对象/函数可以自定义穿梭框的[语义化结构](#semantic-transfer)样式。

## en-US

You can customize the [semantic dom](#semantic-transfer) style of transfers by passing objects/functions through `classNames` and `styles`.
