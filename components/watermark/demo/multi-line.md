## zh-CN

通过 `content` 设置字符串数组或 `WatermarkText` 数组指定多行文字水印内容，并可单独调整每行文字样式。

## en-US

Use `content` to set a string array or `WatermarkText` array to specify multi-line text watermark content with custom font styles per line.
