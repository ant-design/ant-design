## zh-CN

通过 `content` 设置字符串和 `WatermarkText` 组成的数组指定多行文字水印内容，并可单独调整每行文字样式。

## en-US

Use `content` to set an array of strings and `WatermarkText` to specify multi-line text watermark content with custom font styles per line.
