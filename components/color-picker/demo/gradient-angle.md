## zh-CN

通过对象形式的 `angle` 设置线性渐变角度，并通过 `showGradientAngle` 显示角度编辑器。

## en-US

Use `angle` in the object form to set the linear gradient angle, and set `showGradientAngle` to display the angle editor.
