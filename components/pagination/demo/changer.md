## zh-CN

改变每页显示条目数。通过 `sizeChangerRender` 可以将默认选择器替换为数字输入框等自定义组件。

## en-US

Change `pageSize`. Use `sizeChangerRender` to replace the default selector with a custom component such as a number input.
