## zh-CN

通过 `classNames` 和 `styles` 自定义 BorderBeam 的语义化结构样式，例如用 `classNames.root` 提供包裹层圆角。

## en-US

Customize BorderBeam semantic structure styles with `classNames` and `styles`, for example by providing the wrapper radius through `classNames.root`.
