import React from 'react';
import { mount } from 'enzyme';
import List from '..';

describe('List Item Layout', () => {
  it('horizontal itemLayout List which contains string nodes should not be flex container', async () => {
  });

  it('horizontal itemLayout List should accept extra node', async () => {
  });

  it('horizontal itemLayout List should be flex container defaultly', async () => {
  });

  it('vertical itemLayout List should', async () => {
  });
});
